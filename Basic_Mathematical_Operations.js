

function basicOp(operator, value1, value2){
    if (operator == '+'){
        return value1+value2;
        }
    if (operator == '-'){
        return value1-value2;
        }
    if (operator == '*'){
        return value1*value2;
        }
    if (operator == '/'){
        return value1/value2;
        }
}
